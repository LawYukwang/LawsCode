#include <iostream>
#include <stdio.h>

int main()
{
	int a = 88;
	int b;
	long c = (long)a;
	b = 0["abcde"];
	printf("%d",3["abcd"]+0x0a);
	while (true)
	{
		b = 1;
		while (true)
		{
			if((b % 3) == 0)
				break;
			b++;
		}
		a++;
		if(a == 99)
			break;
	}
}